# Core dependencies
openai==1.3.5
requests
python-dotenv
Pillow
numpy

# Video processing
moviepy
ffmpeg-python
pydub
opencv-python

# Machine learning and computer vision
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
# dlib  # Requires CMake - install separately if needed
# face_alignment  # Requires dlib
transformers

# Enhanced Animation System (Professional Quality)
diffusers>=0.25.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
xformers>=0.0.20  # Optional but recommended for memory efficiency
huggingface_hub>=0.16.0
einops
omegaconf
ftfy
regex

# Alternative face processing (no CMake required)
mediapipe

# Scientific computing
scipy
