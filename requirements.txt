# Core ML libraries
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers==4.42.4  # Fixed version to resolve bug
tokenizers==0.19.1  # Fixed version to resolve bug
diffusers>=0.34.0
accelerate>=0.33.0
safetensors>=0.4.0
huggingface_hub>=0.30.1
ftfy>=6.1.1  # Required for WAN pipeline text processing
xformers>=0.0.22  # Optimized attention mechanisms for transformers
imageio
imageio-ffmpeg

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
mediapipe>=0.10.0

# Audio processing - Updated for better multilingual support
TTS==0.22.0
torchcodec>=0.8.0  # Required for XTTS models (media loading/decoding)
librosa>=0.10.0
soundfile>=0.12.0
# Audio generation (MusicGen and AudioGen) - Optional
# audiocraft requires PyAV which needs pkg-config to build from source
# Linux: Install system deps first: sudo apt-get install pkg-config libavcodec-dev libavformat-dev libavutil-dev libswscale-dev
# Then: pip install audiocraft>=1.2.0
# Windows: See INSTALL_AUDIOCRAFT.md for installation instructions
audiocraft>=1.2.0  # Uncomment this line after installing system dependencies on Linux, or follow INSTALL_AUDIOCRAFT.md
# Additional audio processing for better language support
numpy>=1.24.0
scipy>=1.10.0

# Video processing
moviepy>=1.0.3
ffmpeg-python>=0.2.0

# Utilities
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
tqdm>=4.65.0
requests>=2.31.0
python-dotenv>=1.0.0

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0