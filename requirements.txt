# Core dependencies
openai==1.3.5
requests
python-dotenv
Pillow
numpy

# Video processing
moviepy
ffmpeg-python
pydub
opencv-python

# Machine learning and computer vision
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
# dlib  # Requires CMake - install separately if needed
# face_alignment  # Requires dlib
transformers

# AI Image Generation (Stable Diffusion)
diffusers[animatediff]
accelerate
safetensors
xformers  # Optional but recommended for memory efficiency
imageio-ffmpeg  # For AnimateDiff video export

# Alternative face processing (no CMake required)
mediapipe

# Scientific computing
scipy
